---
const galeria = [
  {
    img: "/galeria-1.png",
    title: "Consultas Personalizadas",
    desc: "Valoramos tu salud con evaluaciones nutricionales a medida y atención cercana, para ayudarte a lograr tus metas personales.",
  },
  {
    img: "/galeria-2.jpg",
    title: "Instalaciones Modernas",
    desc: "Disfruta de un ambiente cómodo y equipado con la mejor tecnología para un acompañamiento profesional y seguro.",
  },
];
---

<section id="galeria" class="w-full bg-[#f6fbf6] py-16">
  <div class="max-w-6xl mx-auto px-4">
    <div class="flex flex-col items-center gap-3 mb-4">
      <span
        class="bg-green-700 text-white text-sm font-semibold px-4 py-1 rounded-full shadow"
        >Galería</span
      >
      <h2 class="text-5xl font-bold text-gray-800 text-center">
        Un espacio pensado para tu bienestar
      </h2>
      <p class="text-lg text-gray-800 text-center max-w-2xl">
        “Todo lo que necesitas, en un espacio cómodo y profesional”
      </p>
    </div>

    <div class="flex flex-col md:flex-row gap-8 mt-8">
      {
        galeria.map((item) => (
          <div class="relative flex-1 rounded-3xl overflow-hidden min-h-[280px] shadow-lg group cursor-pointer">
            <img
              src={item.img}
              alt={item.title}
              class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300"
              width="600"
              height="340"
              loading="lazy"
            />
            <div class="absolute inset-0 bg-black bg-opacity-40" />
            <div class="absolute bottom-0 left-0 right-0 p-6 z-10 flex flex-col">
              <h3 class="text-xl md:text-2xl font-bold text-white drop-shadow mb-1">
                {item.title}
              </h3>
              <p
                class="text-white text-base drop-shadow mt-1
                opacity-0 group-hover:opacity-100 translate-y-3 group-hover:translate-y-0 transition-all duration-300"
              >
                {item.desc}
              </p>
            </div>
          </div>
        ))
      }
    </div>
  </div>
</section>
